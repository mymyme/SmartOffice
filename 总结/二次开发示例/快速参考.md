# Monaco Editor 二次开发快速参考

## 🚀 快速开始

### 1. 环境准备
```bash
# 检查环境
node --version  # 需要 18+
npm --version   # 需要 10+

# 进入项目目录
cd /media/storage/project/aicode
```

### 2. 启动开发环境
```bash
# 使用启动脚本
./二次开发示例/启动开发环境.sh

# 或手动启动
cd website
npm install --legacy-peer-deps
npm run dev
```

### 3. 访问应用
- **开发模式**: http://localhost:3007
- **生产模式**: http://localhost:3006
- **示例模式**: http://localhost:3005

## 📁 项目结构

```
aicode/
├── website/                    # 主要前端应用
│   ├── src/website/           # React 组件
│   ├── dist/                  # 构建输出
│   └── package.json           # 依赖配置
├── 二次开发示例/              # 开发示例
│   ├── 基础示例/              # 基础功能
│   ├── 高级示例/              # 高级功能
│   ├── 自定义扩展/            # 自定义功能
│   └── 启动开发环境.sh        # 启动脚本
└── src/                       # Monaco Editor 源码
```

## 🛠️ 核心组件

### MonacoEditor 组件
```typescript
import { MonacoEditor } from './components/monaco/MonacoEditor';

<MonacoEditor
    value={code}
    language="javascript"
    theme="vs-dark"
    onChange={handleChange}
    onEditorReady={handleEditorReady}
/>
```

### 状态管理 (MobX)
```typescript
import { observable, action } from 'mobx';

class EditorState {
    @observable code = '';

    @action setCode(code: string) {
        this.code = code;
    }
}
```

### 自定义语言
```typescript
import { registerMyCustomLanguage } from './custom-language';

// 注册自定义语言
registerMyCustomLanguage();
```

## 🎨 主题配置

### 内置主题
- `vs` - 浅色主题
- `vs-dark` - 深色主题
- `hc-black` - 高对比度

### 自定义主题
```typescript
import { registerCustomThemes } from './custom-theme';

// 注册自定义主题
registerCustomThemes();
```

## ⌨️ 快捷键

### 默认快捷键
- `Ctrl+F` - 查找
- `Ctrl+H` - 替换
- `Ctrl+Z` - 撤销
- `Ctrl+Y` - 重做
- `Ctrl+A` - 全选
- `Ctrl+S` - 保存

### 自定义快捷键
```typescript
import { setupCustomKeybindings } from './custom-keybindings';

// 设置自定义快捷键
setupCustomKeybindings();
```

## 🔧 开发工具

### 构建命令
```bash
# 开发构建
npm run dev

# 生产构建
npm run build-webpack

# 自定义构建
npx webpack --config webpack-simple.config.ts --mode production
```

### 调试技巧
1. 使用浏览器开发者工具
2. 检查 Monaco Editor 控制台输出
3. 使用 React DevTools
4. 检查 MobX 状态变化

## 📚 常用 API

### 编辑器操作
```typescript
// 获取编辑器实例
const editor = monaco.editor.create(container, options);

// 设置值
editor.setValue('new code');

// 获取值
const code = editor.getValue();

// 格式化
editor.getAction('editor.action.formatDocument')?.run();

// 设置语言
monaco.editor.setModelLanguage(model, 'javascript');

// 设置主题
monaco.editor.setTheme('vs-dark');
```

### 语言服务
```typescript
// 注册语言
monaco.languages.register({ id: 'myLanguage' });

// 设置语言配置
monaco.languages.setLanguageConfiguration('myLanguage', config);

// 设置词法分析器
monaco.languages.setMonarchTokensProvider('myLanguage', definition);
```

### 补全提供者
```typescript
monaco.languages.registerCompletionItemProvider('javascript', {
    provideCompletionItems: (model, position) => {
        return { suggestions: [] };
    }
});
```

## 🐛 常见问题

### 1. 构建错误
```bash
# 清理缓存
rm -rf node_modules package-lock.json
npm install --legacy-peer-deps
```

### 2. 类型错误
```bash
# 检查 TypeScript 配置
npx tsc --noEmit
```

### 3. 运行时错误
- 检查浏览器控制台
- 确保 Monaco Editor 正确加载
- 检查组件 props 类型

### 4. 性能问题
- 使用 React.memo 优化渲染
- 合理使用 MobX 的 computed
- 避免不必要的重新渲染

## 📖 学习资源

### 官方文档
- [Monaco Editor API](https://microsoft.github.io/monaco-editor/api/index.html)
- [React 文档](https://reactjs.org/docs)
- [MobX 文档](https://mobx.js.org/README.html)

### 示例代码
- 查看 `二次开发示例/` 目录
- 参考现有组件实现
- 学习 playground 示例

### 社区资源
- GitHub Issues
- Stack Overflow
- Monaco Editor 社区

## 🎯 开发建议

### 1. 组件设计
- 使用 TypeScript 进行类型安全
- 遵循 React 最佳实践
- 使用 MobX 进行状态管理
- 保持组件的单一职责

### 2. 代码组织
- 按功能模块组织代码
- 使用绝对路径导入
- 保持文件结构清晰
- 添加适当的注释

### 3. 性能优化
- 使用 React.memo 优化渲染
- 合理使用 MobX 的 computed 和 action
- 避免不必要的重新渲染
- 使用懒加载减少初始包大小

### 4. 错误处理
- 使用 try-catch 处理异步操作
- 添加错误边界组件
- 提供用户友好的错误信息
- 记录错误日志

## 🚀 部署指南

### 开发环境
```bash
npm run dev
# 访问 http://localhost:3007
```

### 生产环境
```bash
npm run build-webpack
node website-server.js
# 访问 http://localhost:3006
```

### 静态部署
```bash
# 构建静态文件
npm run build-webpack

# 部署到静态服务器
# 将 dist/ 目录上传到服务器
```

---

**开始您的 Monaco Editor 二次开发之旅吧！** 🎉
